127.0.0.1 {{ hostname }}
127.0.0.1 localhost.localdomain localhost
127.0.0.1 localhost4.localdomain4 localhost4
{{ ansible_default_ipv4.address }}  {{ hostname }}

{% for host in groups['master'] %}
{{ host }}  {{ hostvars[host].clusterName }}
{% if not loop.last %}
{% endif %}
{% endfor %}

