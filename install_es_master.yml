---
  - hosts: "{{ run | default('master') }}"
    #- hosts: "{{ run | default('Allnode') }}"]
    user: "{{ user }}"
    roles:
      - system
      - disks
      - elk
    vars_files:
      - ./inventories/{{ env }}/group_vars/global.yml
      - ./inventories/{{ env }}/group_vars/{{ project_name }}.yml
    vars:
      - el_memen: 16
      - disks_data:
          /dev/vdb: /home/data

#  - hosts: "{{ run | default('exporter') }}"
#      roles:
#        - system
#        - java-elk
#      vars_files:
#        - ./inventories/{{ env }}/group_vars/global.yml
#        - ./inventories/{{ env }}/group_vars/{{ project_name }}.yml
